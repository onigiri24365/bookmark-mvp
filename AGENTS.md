# AGENTS.md

このドキュメントは、AIエージェントがこのリポジトリで作業する際のガイドラインを定義します。

## プロジェクト概要

Android共有ブックマークアプリ（Flutter）のMVPを開発するプロジェクトです。

### 技術スタック

- **クライアント**: Flutter（Android）
- **バックエンド**: TypeScript + Hono（Cloud Run）
- **データストア**: Firestore
- **ジョブキュー**: Cloud Tasks
- **インフラ管理**: Terraform
- **LLM要約**: Gemini

### ディレクトリ構成（予定）

```
apps/
  flutter/          # Flutterアプリ
services/
  api/              # Cloud Run API
  worker/           # Cloud Run Worker（Cloud Tasks起動）
infra/
  terraform/        # インフラコード
docs/               # 設計ドキュメント
```

## 開発方針

### 1. 仕様駆動開発

**すべての実装は `docs/` 以下のドキュメントに基づいて行うこと。**

| ドキュメント | 内容 |
|-------------|------|
| `docs/android-share-bookmark-mvp.md` | MVP要件・設計の全体像 |
| `docs/datastore-design.md` | Firestoreのデータモデル設計 |
| `docs/architecture-decisions.md` | アーキテクチャ・運用方針 |

#### 仕様駆動の原則

1. **実装前に仕様を確認する**: コードを書く前に、関連するドキュメントを読み、要件を把握する
2. **仕様との乖離を避ける**: ドキュメントに記載されていない機能を勝手に追加しない
3. **仕様変更が必要な場合は先にドキュメントを更新する**: 実装とドキュメントの整合性を保つ
4. **未決定事項は勝手に決めない**: `docs/android-share-bookmark-mvp.md` の「未決定」セクションに記載された項目は、実装前に確認を取る

### 2. テスト駆動開発（TDD）

**RED → GREEN → リファクタ** のサイクルに従って開発を進める。

#### TDDサイクル

```
┌─────────────────────────────────────────────────────────────┐
│  1. RED（失敗するテストを書く）                               │
│     - 仕様に基づいてテストケースを作成                        │
│     - テストが失敗することを確認                              │
├─────────────────────────────────────────────────────────────┤
│  2. GREEN（テストを通す最小限のコードを書く）                  │
│     - テストを通すことだけに集中                              │
│     - 過剰な実装をしない                                     │
├─────────────────────────────────────────────────────────────┤
│  3. リファクタ（コードを改善する）                            │
│     - テストが通ったまま、コードの品質を向上                   │
│     - 重複の排除、命名の改善、構造の整理                      │
└─────────────────────────────────────────────────────────────┘
```

#### TDDの実践ルール

1. **テストなしにコードを書かない**: 新しい機能・修正は必ずテストから始める
2. **一度に1つのテストだけを失敗させる**: 複数のテストを同時に追加しない
3. **最小限の実装**: GREENフェーズでは、テストを通すために必要な最小限のコードのみを書く
4. **リファクタ時はテストを変更しない**: リファクタ中にテストが失敗したら、実装のバグ
5. **テストも仕様から導出する**: テストケースは `docs/` のドキュメントに記載された要件から導出する

#### テストの種類と優先度

| 種類 | 対象 | 優先度 |
|-----|------|-------|
| ユニットテスト | 個々の関数・クラス | 高 |
| インテグレーションテスト | API エンドポイント、Firestore操作 | 高 |
| E2Eテスト | Flutter UIフロー | 中 |

### 3. コーディングガイドライン

#### 全般

- コミットメッセージは日本語または英語で簡潔に
- 1つのコミットは1つの論理的な変更単位
- PRを作成する際は、関連するドキュメントへの参照を含める

#### Flutter

- `flutter analyze` でエラー・警告がないこと
- `flutter test` で全テストが通ること

#### Cloud Run API / Worker（TypeScript + Hono）

- `npm run lint` / `npm run typecheck` でエラーがないこと
- `npm test` で全テストが通ること
- Firestore操作はトランザクションを適切に使用する
- エラーハンドリングを明確に行う
- 型安全性を重視し、`any` の使用を避ける

#### Terraform

- `terraform fmt` でフォーマット済みであること
- `terraform validate` でエラーがないこと

## AIエージェントへの指示

### 作業開始時

1. まず `docs/` 以下のドキュメントを読み、要件を把握する
2. `README.md` の実装TODOを確認し、現在の進捗を理解する
3. 作業対象の機能に関連する仕様を特定する

### 新機能の実装時

1. **仕様確認**: `docs/` のドキュメントで要件を確認
2. **テスト作成（RED）**: 仕様に基づいてテストを作成し、失敗を確認
3. **実装（GREEN）**: テストを通す最小限のコードを実装
4. **リファクタ**: テストを維持しながらコードを改善
5. **ドキュメント更新**: 必要に応じて `README.md` のTODOを更新

### バグ修正時

1. **再現テスト（RED）**: バグを再現するテストを作成
2. **修正（GREEN）**: バグを修正し、テストを通す
3. **リグレッションテスト**: 既存のテストが引き続き通ることを確認

### コードレビュー観点

- [ ] 仕様（`docs/`）と実装が一致しているか
- [ ] テストが存在し、適切なケースをカバーしているか
- [ ] TDDサイクル（RED → GREEN → リファクタ）に従っているか
- [ ] 不要な機能追加（スコープクリープ）がないか

## 禁止事項

- `docs/` のドキュメントに記載されていない機能の勝手な追加
- テストなしでの機能実装
- 「未決定」とされている仕様の独断での決定
- セキュリティに関わる情報（APIキー等）のコードへの直接埋め込み

## 参考リンク

- [MVP要件・設計](docs/android-share-bookmark-mvp.md)
- [データストア設計](docs/datastore-design.md)
- [アーキテクチャ決定事項](docs/architecture-decisions.md)
